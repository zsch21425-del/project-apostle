// =============================================================
// obj_pause_menu — Create
// Place ONE in each gameplay room. Esc / Start toggles the
// `paused` flag, which player + enemy + controller code respect.
// =============================================================

paused = false;
cursor = 0;
options = ["RESUME", "RESTART LEVEL", "RETURN TO TITLE"];
